// Room for the spinner on a first load, when the section has no rows behind it yet. About two rows
export const SETTINGS_LIST_LOADING_MIN_HEIGHT_PX = 128

// Matched to the card the sections sit on, so the overlay hides the rows rather than tinting them
export const SETTINGS_LIST_LOADING_OVERLAY_CLASS = 'absolute inset-0 z-10 flex items-center justify-center bg-[var(--app-surface-soft)]'
