name: Docker build and browser suite

# Reusable step invoked by the pull request pipeline, it does not trigger on its own
on:
  workflow_call:

permissions:
  contents: read

env:
  # Fixed local tag the stack below reuses, it matches the image name in docker/compose.yml
  IMAGE_REF: luminahq/lumina-finance:ci

jobs:
  build:
    name: Build Docker image (${{ matrix.platform }})
    runs-on: ${{ matrix.runner }}

    # Each leg builds on a runner of its own architecture, so neither is emulated. One
    # architecture failing must not cancel the other, since knowing whether the break is
    # architecture specific is the point of running both
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: amd64
            runner: ubuntu-latest
          - platform: arm64
            runner: ubuntu-24.04-arm

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Docker build
        uses: ./.github/actions/docker-build-setup
        with:
          registry-username: ${{ vars.DOCKERHUB_USERNAME }}
          registry-token: ${{ secrets.DOCKERHUB_TOKEN }}

      # Export once per architecture so each device job tests the same image without rebuilding it
      - name: Build image
        uses: docker/build-push-action@v7
        env:
          # The build record artifact is unused here and would outlive the job that made it
          DOCKER_BUILD_RECORD_UPLOAD: false
        with:
          context: .
          file: ./docker/Dockerfile
          platforms: linux/${{ matrix.platform }}
          push: false
          outputs: type=docker,dest=${{ runner.temp }}/lumina-image.tar
          tags: ${{ env.IMAGE_REF }}
          build-args: |
            APP_VERSION=ci

      - name: Share the built image with browser jobs
        uses: actions/upload-artifact@v7
        with:
          name: browser-image-${{ matrix.platform }}
          path: ${{ runner.temp }}/lumina-image.tar
          if-no-files-found: error
          retention-days: 1
          compression-level: 0

  browser:
    name: Browser suite (${{ matrix.target.platform }}, ${{ matrix.project }})
    needs: build
    runs-on: ${{ matrix.target.runner }}
    strategy:
      fail-fast: false
      matrix:
        target:
          - platform: amd64
            runner: ubuntu-latest
          - platform: arm64
            runner: ubuntu-24.04-arm
        project: [desktop, tablet, phone]

    # Every matrix job starts its own application and database on its runner
    env:
      APP_IMAGE_TAG: ci
      DB_HOST: postgres
      DB_PORT: "5432"
      DB_NAME: lumina
      DB_USER: lumina
      DB_PASSWORD: lumina-smoke

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Download the native image
        uses: actions/download-artifact@v8
        with:
          name: browser-image-${{ matrix.target.platform }}
          path: ${{ runner.temp }}

      - name: Load the image
        run: docker load --input "${RUNNER_TEMP}/lumina-image.tar"

      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Write the operator env file
        run: |
          cat > docker/.env <<EOF
          DB_HOST=${DB_HOST}
          DB_PORT=${DB_PORT}
          DB_NAME=${DB_NAME}
          DB_USER=${DB_USER}
          DB_PASSWORD=${DB_PASSWORD}
          EOF

      - name: Start the stack
        run: docker compose -f docker/compose.yml up -d

      # Installed while the container migrates, applies row-level security and seeds its
      # currencies and categories, so the poll below usually passes on its first attempt
      # rather than waiting out a boot that could have been overlapped
      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version: "26.5"
          cache: npm
          cache-dependency-path: e2e/package-lock.json

      - name: Install the browser suite
        working-directory: e2e
        run: npm ci --ignore-scripts

      # Nothing else type-checks this package: there is no root tsconfig, and the frontend's
      # covers its own source and tests alone. It runs here rather than beside the suite so a
      # type error is reported before the browser download and the health poll it does not need
      - name: Type-check the browser suite
        working-directory: e2e
        run: npm run typecheck

      - name: Install the browser it drives
        working-directory: e2e
        run: npx playwright install --with-deps chromium

      # Caddy serves the backend health route under /api, poll until the app reports ready
      - name: Ping the health endpoint
        run: |
          for attempt in $(seq 1 30); do
            if curl -fsS http://localhost:8080/api/health | grep -qE '"status":\s*"ok"'; then
              echo "Health check passed"
              exit 0
            fi
            echo "Attempt ${attempt}: app not ready yet"
            sleep 5
          done
          echo "Health check never succeeded, dumping logs"
          docker compose -f docker/compose.yml logs
          exit 1

      - name: Run the browser suite
        id: browser-suite
        working-directory: e2e
        env:
          E2E_BASE_URL: http://localhost:8080
        run: npm test -- --project=${{ matrix.project }} --workers=2

      # Gated on the suite rather than on the job, because a report only exists when the suite
      # itself ran. On an image that never boots the poll above fails first, and an upload
      # gated on the job would then find nothing and pass with a warning
      - name: Keep the report from a failed run
        if: ${{ failure() && steps.browser-suite.outcome == 'failure' }}
        uses: actions/upload-artifact@v7
        with:
          name: browser-suite-${{ matrix.target.platform }}-${{ matrix.project }}
          path: e2e/playwright-report
          retention-days: 7

      - name: Tear down the stack
        if: always()
        run: docker compose -f docker/compose.yml down -v
